{
  "name": "seqdiag",
  "version": "0.0.5",
  "description": "Draw network sequence diagram SVGs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "prepublishOnly": "npm run compile",
    "compile": "npm run clean && tsc -p .",
    "watch": "tsc -w -p .",
    "clean": "rimraf ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bugs": {
    "url": "https://github.com/anthony-c-martin/seqdiag/issues"
  },
  "repository": {
    "type": "git",
    "url": "http://github.com/anthony-c-martin/seqdiag.git"
  },
  "keywords": [
    "network sequence diagram",
    "sequence diagram",
    "svg"
  ],
  "author": "Anthony Martin <anthony.ct.martin@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "preact": "^10.5.7"
  }
}
